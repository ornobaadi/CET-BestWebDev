/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'localhost:5000'], // Add 'localhost' and any other domains as needed
  },
  // Other Next.js configurations can go here
};

export default nextConfig;
